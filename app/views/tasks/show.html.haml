= task_list_form(@current_project,TaskList.new) if @current_project.editable?(current_user)
= task_header(@current_project,@task_list,@task)
= task_form(@current_project,@task_list,@task) unless @task.closed?
= people_watching(@current_project,current_user,@task)
= render 'activities/thread', :thread => @task

= javascript_projects_people [@current_project]

- content_for :column do
  = task_list_column(@current_project,@task)
